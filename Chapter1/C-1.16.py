""" From page 5, the compount assignment ( data[j] *= factor ) 
    is the short notation used for data[j] = data[j] * factor
    which reassigns data[j] to the new value not mutating 
    the object but breaking the alias and reassigning it 
    to the newly computed value.     

    https://goo.gl/IsSBDI - visualized execution              """